subprojects {
    ext.version_number     = "2.6.1"
    ext.group_info         = "org.whispersystems"
    ext.curve25519_version = "0.4.1"

    if (JavaVersion.current().isJava8Compatible()) {
        allprojects {
            tasks.withType(Javadoc) {
                options.addStringOption('Xdoclint:none', '-quiet')
            }
        }
    }
}
